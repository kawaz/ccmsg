node_modules/
# lock は bun.lock。npm publish 系コマンドが副産物として作るものは捨てる
package-lock.json
# playwright-cli skill がセッション中に落とす snapshot / console log
.playwright-cli/
# 各自の VS Code workspace ファイル (ccmsg@<worktree>.local.code-workspace)。
# oxfmt はユーザ global の ignore を読まないので、ここに書かないと lint が拾う
*.local.*
